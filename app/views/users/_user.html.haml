%li  
  = gravatar_for user, size: 52
  = link_to user.common_name, user
  - if current_user.admin? && !current_user?(user) 
    | 
    = link_to "delete", user_path(:id => user.id, :page => params[:page]), method: :delete, data: { confirm: "You sure?", :page => params[:page] }
